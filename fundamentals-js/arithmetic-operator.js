const [a, b, c, d] = [3, 5, 1, 15];

const sum = a + b + c + d;
const sub = d - b;
const multiplication = a * b;
const division = d / a;
const remainder = a % 2;

console.log(sum, sub, multiplication, division, remainder); // 24 10 15 5 1
